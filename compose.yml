services:
  parser:
    image: ghcr.io/magnusakselvoll/abanca-credit-card-statement-parser:latest
    volumes:
      - ./input:/data/input:ro
      - ./output:/data/output
    # Env vars are baked into the image as defaults; override here if needed:
    # environment:
    #   AbancaCardParser__InputDir: /data/input
    #   AbancaCardParser__OutputDir: /data/output
    #   AbancaCardParser__LogDir: /data/output
