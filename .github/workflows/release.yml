name: Release

on:
  release:
    types: [published]

permissions:
  contents: write
  packages: write

jobs:
  publish-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive version from git tag
        id: tags
        run: |
          TAG="${GITHUB_REF_NAME}"     # e.g. v0.1.1
          VERSION="${TAG#v}"            # e.g. 0.1.1
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Push versioned image
        run: |
          dotnet publish src/FireflyIiiCustomUploader.Web \
            -c Release \
            /t:PublishContainer \
            /p:ContainerRegistry=ghcr.io \
            /p:ContainerImageTag=${{ steps.tags.outputs.version }}

      - name: Push latest image
        run: |
          dotnet publish src/FireflyIiiCustomUploader.Web \
            -c Release \
            /t:PublishContainer \
            /p:ContainerRegistry=ghcr.io \
            /p:ContainerImageTag=latest

      - name: Add Docker image reference to release notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
          VERSION: ${{ steps.tags.outputs.version }}
          IMAGE: ghcr.io/${{ github.repository }}
        run: |
          BODY="$(gh release view "$TAG" --json body -q .body)"
          if printf '%s' "$BODY" | grep -qF '## Docker image'; then
            echo 'Docker image section already present; skipping.'
            exit 0
          fi
          {
            printf '%s\n\n' "$BODY"
            printf '## Docker image\n\n'
            printf '```\n'
            printf 'docker pull %s:%s\n' "$IMAGE" "$VERSION"
            printf '```\n\n'
            printf 'Also published as `%s:latest`.\n' "$IMAGE"
          } > release-notes.md
          gh release edit "$TAG" --notes-file release-notes.md
